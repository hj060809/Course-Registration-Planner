generator client {
  provider = "prisma-client-js"
  output   = "../generated/client"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model AdminCode {
  id    Int     @id @default(autoincrement())
  code  String  @unique
  name  String
}